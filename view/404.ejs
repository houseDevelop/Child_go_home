<%- include('templates/header',{tituloweb:'Child go Home',value:4})%>
<style>
  body {
      margin: 0;
      padding: 0;
  }

  header {
      /* Estilos para el encabezado (header) */
  }

  #video-container {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      overflow: hidden;
      z-index: -1; /* Coloca el video detrás del contenido */
  }

  #background-video {
      object-fit: cover;
      width: 100%;
      height: 100%;
  }

  .contenido {
      position: relative;
      z-index: 1;
      /* Estilos para el contenido */
  }
</style>


<body>
  <div id="video-container">
      <video autoplay loop muted id="background-video">
          <source src="/images/recursos/Error.mp4" type="video/mp4">
          Tu navegador no soporta la reproducción de videos.
      </video>
      <div class="contenido">
          <!-- Contenido adicional de tu página -->
      </div>
  </div>

  
    <%- include('templates/footer' ,{tituloweb:"Child go Home",value:3})%>